#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// Funções para cada algoritmo (ShellSort, MergeSort, SelectionSort, QuickSort, BucketSort, RadixSort)
// ... (implementações detalhadas)

int main() {
    int n = 10000; // Tamanho da lista
    int arr[n];

    // Inicializar a lista com valores aleatórios ou ordenados

    // Medir o tempo e número de comparações para cada algoritmo
    clock_t start, end;
    int comparacoes = 0;

    // Shell Sort
    start = clock();
    comparacoes = 0;
    shellSort(arr, n);
    end = clock();
    // ... (calcular e imprimir tempo e comparações)

    // Merge Sort
    // ... (repetir para os outros algoritmos)

    return 0;
}
